Contact
{
	actions: edit, delete;
	orderBy: name;
	filter: name;
	props: -id, action.edit, action.delete, firstName, lastName, phoneNumber, category{fieldType: wicket.contrib.webbeans.databinder.examples.CategoryField};
}
Category
{
	orderBy: "name desc";
	props: -id, name;
}

